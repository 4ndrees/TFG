Ejecuta estos comandos para verificar la versión de Node.js y npm:

node -v
npm -v

nvm use 20.18.3

instala dependencias del proyecto, esto instalara todas las dependencias listadas en el package.json

npm install

para correr la app node server.js


ahora mismo se llega con la ruta http://localhost:3000 desde 
el ordenador en el que se lance 


se necesita instalar pip install tqdm
tb pip install torch torchvision torchaudio

se necesita tb instalar tensorflow

cfreo que podemos utilizar esto
try:
    import torch
    import tqdm
except ImportError:
    import sys
    !{sys.executable} -m pip install torch torchvision torchaudio tqdm
